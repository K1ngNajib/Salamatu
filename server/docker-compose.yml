services:
  db:
    image: mongo:latest
    ports:
      - "27018:27018"
    container_name: mongodb_commandlink
    volumes:
      - /data/mongodb_commandlink:/data/db

# MongoDB commands
# 1. mongosh mongodb+srv://Commandlink:<First23@>@cluster0.krayzwv.mongodb.net/
# 2. show dbs
# 3. use <db_name>
# 4. show collections
# 5. db.<collection_name>.find()
# 6. db.<collection_name>.find().pretty()